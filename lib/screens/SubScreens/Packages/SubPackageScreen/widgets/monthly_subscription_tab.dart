
import 'package:flutter/material.dart';

class MonthlySubscriptionTab extends StatelessWidget {
  const MonthlySubscriptionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
